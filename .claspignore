# O clasp resolve estes padroes de forma relativa ao rootDir (src/),
# nao a raiz do repositorio. Bloqueia tudo e libera so o que deve
# mesmo ir para o Google: o manifesto e o codigo do script (.js hoje,
# .ts depois da migracao).
**/**
!appsscript.json
!**/*.js
!**/*.ts
